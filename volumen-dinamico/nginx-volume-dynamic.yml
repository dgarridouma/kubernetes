apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment-dynamic
  labels:
    app: nginx-dynamic
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-dynamic
  template:
    metadata:
      labels:
        app: nginx-dynamic
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        volumeMounts:
        - name: web-storage
          mountPath: /usr/share/nginx/html
      volumes:
      - name: web-storage
        persistentVolumeClaim:
          claimName: web-pvc-dynamic
---
apiVersion: v1
kind: Service
metadata:
  name: web-service-dynamic
  labels:
    app: nginx-dynamic
spec:
  type: NodePort
  selector:
    app: nginx-dynamic
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30083
